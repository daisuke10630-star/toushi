# 次にやること（作業引き継ぎメモ）

最終更新: 2026-07-26

## サーバーの起動

**PowerShellを2つ開いて**、それぞれで実行してください。エージェントがバックグラウンドで
起動したプロセスはセッションが切れると落ちます。「Failed to fetch」は大抵これが原因です。

```
cd C:\Users\daisuke\Desktop\stock-analyzer\backend; .\venv\Scripts\Activate.ps1; uvicorn app.main:app --reload --port 8000
```
```
cd C:\Users\daisuke\Desktop\stock-analyzer\frontend; npm run dev
```

## 検証の到達点（2026-07-26 時点）

10年・25銘柄・往復コスト0.1%込みで検証した結論です。

- **どの設定も単純保有を上回りません。** 現行設定のベンチ比は前半-0.03% / 後半-0.40%。
- **★に優位性はありません。** ★3/4/5でベンチ比の符号がばらつき、ノイズと区別がつきません。
- **ただしエントリー方式の改善は本物でした。** 旧版の押し目待ちは -3.6%/回（勝率22%）で、
  ベンチ比 -5.0%。高値ブレイクへの変更で -0.4% まで改善しました。
  「勝てるようになった」のではなく「大きく負けるのをやめた」段階です。
- 2年データでは +1.59%/+1.60% と勝って見えましたが、**10年で消えました（過学習）**。

現在の設定：高値ブレイク（20日高値）/ ATR×3損切り / 利確RR1:3 / 保有15日。
スクリーナーは「その銘柄で単純保有を上回っているか」で足切りします（246銘柄中6銘柄が該当）。

## 検証済み・やり尽くした項目

- 損切り：固定1〜10% / ATR×1〜3 → ATR×3が最良だが優位性は出ない
- 利確：BB±σ / リスクリワード比1:1〜1:3 / 固定% → RR1:3が最良
- 保有期間：5/10/15/20/30日 → 2年では15日、10年では差なし
- エントリー：押し目待ち / 翌日成行 / 高値ブレイク → ブレイクが最良
- ブレイクアウト窓幅：10/20/40日 → 差なし
- ★の水準：3/4/5 → 優位性なし
- 時間軸：日足 / 5分足 / 1分足 → 分足は削除済み

**パラメータ調整では優位性は出ない、というのが現時点の結論です。**

## 次に試すなら（ルール本体の作り直し）

1. **★の条件を個別に検証する**
   `signals.py` の加減点（パーフェクトオーダー+2、RSI過熱-1、ダブルトップ-1 など）を
   1つずつ単独でベンチ比を測る。★全体では効かなくても、特定の条件だけが
   効いている（あるいは足を引っ張っている）可能性があります。

2. **保有期間ではなく「手仕舞い条件」を変える**
   現在は固定バー数で強制手仕舞い。トレーリングストップ（高値から一定%下がったら手仕舞い）
   に変えると、伸びる銘柄を伸ばせる可能性があります。

3. **母集団を広げる**
   現在は日経225中心の246銘柄。小型株・新興市場のほうがテクニカルが効く可能性があります。
   `universe.py` を編集してください。

4. **そもそもテクニカル以外の要素を入れる**
   出来高急増、決算またぎ、業種の相対強弱など。ここまでの検証は
   「価格と移動平均だけ」で完結しており、情報量が足りていない可能性があります。

## 最重要：資金効率で測ること

**1トレードあたりの期待値も、1日あたりのリターンも、どちらも不十分です。**
どちらも「ポジションを持っている間」しか見ておらず、現金で待っている時間を無視します。

実測（3年・30銘柄・手数料込み）：

| | 年率 |
|---|---|
| 戦略（同時保有3銘柄） | +19.1% |
| 同じ30銘柄を買い持ち | **+25.1%** |

1日あたりの指標では戦略が勝っていましたが、**実際に資金を回すと負けます。**
同時保有1銘柄だと3年で約350日しか投資しておらず、半分以上が現金だったためです。

**新しいルールを評価するときは、必ず日付順のポートフォリオ・シミュレーションまで
やってください。** `backtest.evaluate` の集計だけでは資金効率が測れません。

## 検証の作法（必ず守る）

- 判断軸は勝率ではなく**コスト控除後の期待値**
- **必ず単純保有（`backtest.benchmark`）と比較する。** 上昇相場では何を買っても勝つため、
  絶対値だけ見ると誤認する。実際に2回誤認しかけた
- ベンチマークは戦略と**同じ損切り条件**で計算する。条件が食い違うと誤判定する
  （一度、戦略ATR×3 / ベンチ固定8% のまま比較して20日保有を「勝ち」と誤判定した）
- **必ず10年で検証する。** 2年だと過学習で勝って見える
- 前半70%／後半30%に分け、両方でベンチマークを上回る設定だけ採用する
- 高速化：★・エントリー・BB・ATRは損切り／利確／エントリー方式に依存しない。
  `backtest.collect_events` で一度収集し、`backtest.evaluate` で組み合わせだけ振る
  （エントリー方式も evaluate 側の引数なので、収集は使い回せる）

## 注意点

- **メモリが逼迫しやすい環境です**（全8GB、空き1.3GB程度）。
  スイープは25銘柄程度に抑えないと pandas の import すら MemoryError で落ちます
- スイープ用スクリプトはスクラッチパッドに置いたままでリポジトリには入れていません。
  `optimize.sweep()` / `backtest.evaluate()` を呼ぶ数十行を書き直すのが早いです
- 10年検証は `dataclasses.replace(tf, yf_period="10y", bars=2600)` で行いました

## その他の宿題

- スクリーナーの銘柄名が英語（yfinance の longName）。日本語化にはコード→社名の対応表が必要
- チャート描画（`lightweight-charts` 等）は未着手
