# === 個人情報：絶対に公開しないこと ===
# 保有銘柄・取得単価・株数が入っています
backend/positions.json
# 保有ポジションを含めて生成したレポート
dashboard-snapshot.html

# 環境変数（APIキー等）
backend/.env

# Python
backend/venv/
__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/

# 生成物のうち index.html と report.json はコミットする。
# 相場中の株価更新ワークフローが、公開中のページを取得し直す代わりに
# ここから読むため（CDNが古い版を返して分析結果を巻き戻す事故を防ぐ）。
docs/prices.json
docs/.nojekyll
